$kind: http-request
url: "{{baseUrl}}/api/v1/sales/{{saleUuid}}"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Sale detail retrieved successfully', function () {
          pm.response.to.have.status(200);
          var j = pm.response.json();
          pm.expect(j).to.have.property('saleUuid');
          pm.expect(j).to.have.property('items');
      });
    language: text/javascript
order: 2000
